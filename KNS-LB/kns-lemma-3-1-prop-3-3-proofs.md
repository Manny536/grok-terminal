# KNS(LB): Lemma 3.1 and Proposition 3.3 — Full Proofs

**Status:** Lemma 3.1 `FORMAL` (OB-KNS-3 discharge) · Prop 3.3
`PROPOSED-FOR-CANON` (Q2 HOLD, Grok cross-derivation) · standalone extract of
PAPER-001 §2 · RH OPEN · no proof of RH claimed.

## Setting

Incidence datum 𝒟 = (T, Y): finite family of δ-tubes in B₁ ⊂ ℝ^d with
measurable shadings Y(T) ⊆ T, up to ambient isometry. Imports ([GWZ26] §2):
U(T,Y) = ∪ Y(T); μ(T,Y) = Σ_T |Y(T)| / |U(T,Y)|; pointwise
μ(x) = #{T : x ∈ Y(T)}; Δ(W,K) = Σ_{W⊂K}|W|/|K|; Δ_max = max over convex K.
Overlap statistic S(𝒟) := (μ(T,Y), Δ_max(T), {μ(x)}, |U|).
Placement datum P = (H, A, ψ): Hilbert space, closed register subspace A,
projection π_A, unit ψ; leakage ℓ_off(ψ) = ‖(I−π_A)ψ‖².

## Lemma 3.1 (overlap is a statistic; μ ↛ π_A)

**Claim.** No function F satisfies F(S(𝒟)) = ℓ_off across admissible
encodings. For every 𝒟 and every λ ∈ [0,1] there is an admissible placement
with ℓ_off = λ.

**Proof.**
1. *Measurability.* Every entry of S(𝒟) is defined purely from the sets
{(T, Y(T))} — [GWZ26] eqs. (3), (7) and the pointwise count. The map
𝒟 ↦ S(𝒟) factors through the σ-algebra generated by incidence data; no
Hilbert-space object enters.
2. *State-side freedom.* Fix any H, register A with dim A ≥ 1, dim A^⊥ ≥ 1;
pick unit u ∈ A, v ∈ A^⊥; set ψ_λ = √(1−λ)u + √λ v. The incidence datum is
unchanged; ℓ_off(ψ_λ) = λ.
3. *Register-side freedom.* Dually, fix any canonical encoding ψ(𝒟) (e.g.
the l2c_probe center mode); for each λ rotate A to A_λ with
‖π_{A_λ}ψ‖² = 1 − λ.
Either freedom alone kills any factorization S(𝒟) → ℓ_off. ∎

Corollary: μ and Δ_max underdetermine π_A and Action(C). They are observable
from union footprints; placement is a declaration on the lawful sector.

## Proposition 3.3 (center action unseen in overlap)

**Claim.** μ determines crowding of tube footprints; Action(C) determines
lawful throughput declaration and π_A alignment. μ → ∞ under glare saturation
implies neither Re(s) = ½ zero placement nor RH.

**Proof.**
(i) *Separation:* Lemma 3.1.

(ii) *Quantitative coexistence (executes falsifier F-KNS-1 in both
directions).*
**E1 — fan (max glare, max leakage).** T^C_δ, full shading. Angle counting
(PAPER-001 Lemma 1.1b): μ(x) ≍ (δ+|x−C|)^{−(d−1)}, so
sup μ = Θ(δ^{−(d−1)}) on the core B(C,δ), while μ̄ ≍ 1 and Δ_max ≍ 1.
Declare A ⊥ ψ_fan: ℓ_off = 1. High overlap + total misplacement coexist ⇒
the claim "overlap determines placement" is **falsified** (F-KNS-1 FIRED).
**E2 — Perron family (higher average overlap, no center, zero leakage).**
δ-discretized Perron/Besicovitch family T^B_δ in ℝ²: direction-separated,
Σ|Y| ≍ 1, and |U| ≍ 1/log(1/δ) (`KNOWN`: [Bes28], [Per28]; optimality
[Kei99]) ⇒ μ̄(T^B) ≍ log(1/δ) — strictly larger than the fan's μ̄ ≍ 1 —
with **no common point** and ℓ_off = 0 under an aligned declaration.
Sharp inversion: the all-through-one-point configuration has unit average
overlap; the no-center configuration has divergent average overlap. Hence
neither pointwise nor averaged overlap functionals determine, bound, or are
bounded by ℓ_off.

(iii) *Non-inference to zeros.* An implication μ → (zero placement on
Re s = ½) requires an explicit constructed map (missing rung, convergent
semantics). The only constructed candidate lane — bounded coupling of K_σ —
is `CLOSED-NEGATIVE` (Theorem H, V6.5). Absent a carrier, glare saturation
carries no zero-location content. This clause is `FORMAL` *as a
non-inference* recording current canon state; construction of such a map
would supersede it (a positive result, not a falsification). ∎

**Refs.** [GWZ26] arXiv:2601.14411 §2 · [Bes28]/[Per28]/[Kei99] Perron-tree
area · Theorem H `wp5b-bounded-lane-closure.md` · scaffold
`PEAICE-CLAUDEV6-KNS-LB-SCAFFOLD-001.md` §3.
