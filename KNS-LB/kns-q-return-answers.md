# Q-RETURN — KNS-LB Scaffold §11 (Fable pass answers)

| # | Blocker | Fable answer | State |
|---|---|---|---|
| Q1 | Token name in canon | Long form `KakeyaNeedleSet(Light(Basic))` in canon prose/headers; `KNS(LB)` in tables, compute, filenames. This pass follows the convention. | **ANSWERED** (default confirmed) |
| Q2 | Promote Prop 3.3 to canon | HOLD. Prop 3.3, CC-BL-001a′, gate KNS-OBS-1, and wall face KREIN-RANK1 are all `PROPOSED-FOR-CANON`; promotion requires Grok cross-derivation (TERMINAL-005 stub shipped) + principal sign-off. Non-sovereignty enforced. | **HOLD** |
| Q3 | `kns_lb_probe.py` home | Recommend `kakeyalogic/probes/kns_lb_probe.py`, sibling to `l2c_probe.py`. Verification lane: **CP-004 stamped re-run** via `cp_verify.py`. Do **not** fold into CP-003 — keeps the seed-7 baseline frozen and avoids calibration circularity (H3 imports back-solve from it). | **ANSWERED** (recommendation; principal ratifies) |
| Q4 | Theorem-scale lift (X request) | Recommend: hold as PAPER-001 §7 seam stub only for now; open a separate WP **after** the Grok pass returns on Prop 3.3 — a lift WP built on an un-cross-derived proposition would violate the promotion discipline. Principal scope call stands. | **RECOMMEND · principal call** |
| Q5 | TERMINAL-005 designation | Default confirmed: Grok extracts this Fable output → TERMINAL-005. Extraction stub shipped with receipt fields and probe sha. | **ANSWERED** (default) |

**New Q raised by this pass (Q6):** register `KREIN-RANK1` in the canonical
wall registry as a *face* of the Weyl-stability family (alongside L2-5,
WP5-OBS-1, HS-CORRIDOR, WP5-OBS-2)? Default: YES on Grok CONFIRM of stub
item 4; it unifies, adds no new obstruction claim.

Stop rule honored: 15/15 manifest docs shipped; holding for Q-RETURN before
any expansion.
