# TERMINAL-005 — Grok (xAI) Extraction Stub

**Source terminal:** Claude Fable 5 (Max) · 2026-07-02 ·
`PEAICE-CLAUDEV6-KNS-LB-*` pass (15 docs) · **Role:** cross-derivation
evaluator per non-sovereignty protocol (FLAG-001: evaluator non-sovereignty
grounds h; h < 1).

## Cross-derive (required for Q2 canon promotion)

1. **Lemma 3.1** — μ ↛ π_A separation: verify statistic-measurability +
   both freedom directions (state-side, register-side). Independent proof
   or counterexample.
2. **Prop 3.3** — fan/Perron inversion: re-derive μ(x) ≍ (δ+|x−C|)^{−(d−1)},
   μ̄_fan ≍ 1, μ̄_Perron ≍ log(1/δ) (Keich optimality cite check).
3. **CC-BL-001a split** — re-derive Thm 4.1 (Prékopa route) and confirm the
   two-mode countermodel closed form 1 − (a² − 2abs + b²)/(1 − s²);
   re-run `kns_lb_probe.py` (expect sha 09ef26d3a2eb…8211b011 →
   D5_unimodal_monotone=true, twomode=false, peak 0.9662 @ δ=1.0,
   dense_pass=true @ E_used 3.0406).
4. **KREIN-RANK1 wall face** — audit the [P96] Thm 4.1 / Cor 4.4 /
   Weyl–von Neumann usage; confirm it is a face of the existing
   Weyl-stability family, not a new obstruction claim.
5. **Gate KNS-OBS-1** — confirm gate conditions were met as specified;
   flag any self-certification leakage.

## Receipt fields (return format)

```text
TERMINAL-005 verdict per item: CONFIRM / AMEND(diff) / REFUTE(counterexample)
Probe re-run: sha match Y/N · outputs match Y/N · environment stamp
Canon recommendation: promote {Prop 3.3, CC-BL-001a′, KNS-OBS-1} Y/N per item
Forbidden-list audit: RH-proof / curve-identity / symbol-collapse — clean Y/N
```

Holds until returned: Q2 (canon promotion) · Q4 final scope (theorem-scale
lift WP). Principal sign-off follows Grok receipt. No claim is canon until
both land.
